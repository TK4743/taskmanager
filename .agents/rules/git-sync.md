# Git Sync Rule: Dual GitHub Repository Push

All commits must be pushed to both repositories:
- `https://github.com/TK4743/taskmanager.git`
- `https://github.com/Tharun4743/taskmanager.git`

The `origin` remote has both push URLs configured. Always verify pushes reach both repositories.
